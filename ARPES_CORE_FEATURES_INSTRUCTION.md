# ARPES 智能科研平台核心功能展示 Instruction

## 1. 使用目的

这份 instruction 用于单独介绍和演示 ARPES 智能科研平台的产品定位、设计理念和核心功能。它可以直接交给产品、设计、开发或演示人员使用，不要求把这些介绍文字放进登录页。

展示时必须区分：

- **当前能力**：网站已经具备或已有可操作界面的功能。
- **建设目标**：智能化实验与智能化分析之间的数据互传、实时模型演算和参数闭环反馈。
- 不得把建设中的能力描述成已经正式接入的生产功能。

## 2. 核心叙事

平台由两个相互连接的领域组成：

| 领域 | 定位 | 主要内容 |
| --- | --- | --- |
| 智能化实验 | 实验侧入口 | 仪器、样品、环境参数、实时采集数据和实验任务 |
| 智能化分析 | 分析侧工作台 | 发展历史、材料预测、数据建模、定点调研和分析报告 |

最终目标是形成双向闭环：

`实验数据进入分析侧 → 模型实时演算 → 输出候选结论和参数建议 → 返回实验侧验证 → 新数据再次进入分析侧`

建议在展示画面中将“智能化实验”放在左侧，“智能化分析”放在右侧，中间使用清晰的双向连接表达以下三项建设目标：

1. 数据双向互传
2. 模型实时演算
3. 参数闭环反馈

## 3. 设计理念

平台的核心不是继续堆叠彼此分立的工具，而是为不同学科、不同领域建立可定制的**全需求覆盖 Map**。

每一张 Map 应覆盖完整研究链路：

`提出问题 → 查阅历史 → 预测材料 → 获取和治理数据 → 在线建模 → 定点调研 → 输出报告 → 返回实验验证`

与传统分立模块的区别：

| 传统工具 | 全需求覆盖 Map |
| --- | --- |
| 只解决一个独立问题 | 覆盖一个研究任务的完整过程 |
| 数据、模型和文献彼此割裂 | 数据、模型、文献和报告共享上下文 |
| 输出停留在单次结果 | 结果可继续进入实验验证和下一轮分析 |
| 所有领域使用同一套固定流程 | 根据学科、材料体系和实验类型进行定制 |

## 4. 五个基础功能模块

### 4.1 发展历史（实时更新）

**功能目标**

持续整理关键发现、材料体系、实验条件和研究争议，形成可更新的研究时间线，而不是静态年表。

**展示例子**

- 展示超导材料的 `年份 × 压力 × Tc` 三维历史图。
- 点击某个样品点，查看材料名称、发现时间、实验条件和相关文献。
- 新增研究记录后，时间线和材料分类同步更新。

**预期输出**

研究脉络、关键节点、材料家族变化以及对应的可信来源。

### 4.2 材料预测（智能化重点功能）

**功能目标**

组合元素、晶体结构、历史规律和科学模型，提出未来值得实验验证的候选材料，而不只是查询已有材料。

**展示例子**

- 将 `Mg` 和 `B` 拖入坩埚，展示候选组成、规则预览和不同模型的预测结果。
- 使用晶体结构、Quantum Espresso 和电子能带工具继续验证结构与电子性质。
- 将模型结论标注为“规则预览”“计算结果”或“待实验验证”，避免混淆可信度。

**预期输出**

候选材料、预测理由、置信度、需要补充的计算以及建议实验条件。

### 4.3 数据（历史治理与在线建模）

**功能目标**

治理历史实验数据，同时对当前采集的数据进行在线预览、切片、比较和建模，并为未来与智能化实验实时互传做准备。

**展示例子**

- 导入一份 ARPES HDF5 数据。
- 显示三个二维切片和对应曲线，并保持所有坐标轴数字从小到大。
- 拖动切片位置，四个视图同步更新。
- 将结果保存到本地数据仓库，再次打开时可直接拉回数据预览。

**预期输出**

结构化数据记录、可复现预览、在线模型结果、数据版本和实验来源。

### 4.4 调研（定点找资料）

**功能目标**

围绕指定材料、物理现象、实验条件或争议问题，定点查找论文、图表和证据，而不是只返回泛化搜索结果。

**展示例子**

- 输入“查找 FeSe 的 ARPES 能隙与温度依赖文章”。
- 按材料、元素、研究主题和图表类型筛选结果。
- 打开论文图表，查看来源、题录和与当前材料任务的关系。

**预期输出**

经过筛选的论文清单、关键图表、证据摘要、来源链接和仍需验证的问题。

### 4.5 分析报告

**功能目标**

将实验记录、模型结果、对话内容、表格和研究历史整理为可交付报告。

**展示例子**

- 从当前数据预览和模型结果生成一份实验分析报告。
- 从历史时间线和定点调研结果生成一份材料历史研究报告。
- 输出 PDF、Word、Excel 或 Markdown，并保留数据来源和文献引用。

**预期输出**

实验报告、历史研究调研报告、材料比较表和可追溯的引用清单。

## 5. 推荐的完整演示流程

建议使用一个连续案例展示五个模块，而不是分别打开五个无关页面。

**演示主题：FeSe 系材料的 ARPES 研究任务**

1. 在“发展历史”中查看 FeSe 所属材料家族及相关里程碑。
2. 在“材料预测”中选择 Fe、Se 和可选掺杂元素，比较模型建议。
3. 在“数据”中导入 FeSe 的 HDF5 数据，拖动切片并选择主图。
4. 在“调研”中定点检索 FeSe 能隙、温度依赖和费米面文献。
5. 在“分析报告”中合并数据图、模型结果、文献证据和结论。
6. 将“建议下一组温度或动量扫描参数”标记为未来返回智能化实验侧的闭环输出。

## 6. 展示要求

- 首先讲清楚两个领域，再介绍五个功能模块。
- 强调 Map 是一条完整研究链路，不是五个孤立页面。
- 每个模块至少展示一个真实可理解的操作例子。
- 页面文案应简洁，详细说明放在独立 instruction、演示稿或帮助文档中。
- 所有模型输出必须显示来源、状态和可信度。
- “实时互传”“实时演算”“自动反馈实验参数”在正式接入前统一标注为建设目标。

## 7. 验收清单

- [ ] 左侧能够说明智能化实验的职责。
- [ ] 右侧能够说明智能化分析的职责。
- [ ] 能够解释两侧未来如何进行数据互传和实时演算。
- [ ] 能够说明全需求覆盖 Map 与传统分立工具的区别。
- [ ] 五个模块都有功能目标、展示例子和预期输出。
- [ ] 至少有一个端到端案例贯穿五个模块。
- [ ] 当前能力与未来目标没有混淆。
